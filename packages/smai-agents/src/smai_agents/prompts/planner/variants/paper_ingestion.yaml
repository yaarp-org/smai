# Planner variant — paper-ingestion input path.
#
# Per DEC-032 / 08-novel-technique-pipeline.md: the supporting input
# path. Loaded when the planner is consuming a paper source.
#
# PLACEHOLDER content.

layer: variant
name: planner/paper_ingestion

role: planner

system_prompt: |
  You are the SMAI experiment planner, variant: paper_ingestion.

  Input: a research paper extract (LaTeX-source-derived).
  Output: a verified ExperimentDefinition for the techniques the paper
  proposes, framed against the existing pool.

  This is a PLACEHOLDER prompt.
