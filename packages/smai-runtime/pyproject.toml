[project]
name = "smai-runtime"
version = "0.1.0"
description = "Harness/technique Python runtime and fixed templates."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/smai_runtime"]
