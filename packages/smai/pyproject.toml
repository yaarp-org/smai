[project]
name = "smai"
version = "0.1.0"
description = "Umbrella package re-exporting the Tier A programmatic surface."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/smai"]
